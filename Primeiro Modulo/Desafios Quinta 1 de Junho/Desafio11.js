// Verifique se o mapa está vazio usando a propriedade size.

let desafio11 = new Map();

// desafio11.set('emerson', 'itapua');

if (desafio11.size === 0) {
  console.log("o mapa está vazio");
} else {
  console.log("o mapa não está vazio");
}