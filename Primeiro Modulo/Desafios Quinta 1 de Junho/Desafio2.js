// Adicione três pares chave-valor ao mapa usando o método set().

let dados = new Map();

dados.set('emerson', 'itapua');
dados.set('stephanie', 'cidade nova');
dados.set('luan', 'ipitanga');

console.log(dados);